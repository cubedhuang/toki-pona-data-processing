{
	"type": "module",
	"scripts": {
		"codegen": "bun run ./scripts/grammar-preprocessor.js grammar.ne grammar-processed.ne && nearleyc grammar-processed.ne -o grammar.ts"
	},
	"dependencies": {
		"nearley": "^2.20.1"
	},
	"devDependencies": {
		"@types/bun": "^1.2.8"
	}
}
